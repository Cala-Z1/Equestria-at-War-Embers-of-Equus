CRY_crystal_heart_decisions = {

	# ── Spread the Crystal Heart's Love ──────────────────────────────────────
	CRY_spread_crystal_love = {
		icon = GFX_decision_generic_crystal
		priority = 100

		allowed = { tag = CRY }

		available = {
			tag = CRY
			has_war = no
			NOT = { check_variable = { CRY_corruption_level < 1 } }
		}

		visible = {
			tag = CRY
			NOT = { has_country_flag = CRY_sombra_defeated }
		}

		cost = 50
		days_remove = 30
		fire_only_once = no
		cancel_if_not_visible = yes

		modifier = {
			stability_weekly = -0.005
			political_power_gain = -0.05
		}

		complete_effect = {
			custom_effect_tooltip = CRY_spread_love_tt
		}

		remove_effect = {
			add_to_variable = { CRY_corruption_level = -8 }
			clamp_variable = {
				var = CRY_corruption_level
				min = 0
				max = 100
			}
			add_stability = 0.02
			custom_effect_tooltip = CRY_corruption_decreased_tt
		}

		ai_will_do = {
			factor = 10
		}
	}

	# ── Festival of the Crystal Heart ────────────────────────────────────────
	CRY_crystal_fair = {
		icon = GFX_decision_generic_political_support
		priority = 100

		allowed = { tag = CRY }

		available = {
			tag = CRY
			has_war = no
			has_political_power > 75
			NOT = { check_variable = { CRY_corruption_level < 1 } }
		}

		visible = {
			tag = CRY
			NOT = { has_country_flag = CRY_sombra_defeated }
		}

		cost = 75
		days_remove = 60
		fire_only_once = no

		modifier = {
			consumer_goods_expected_value = 0.03
		}

		complete_effect = {
			custom_effect_tooltip = CRY_crystal_fair_tt
		}

		remove_effect = {
			add_to_variable = { CRY_corruption_level = -15 }
			clamp_variable = {
				var = CRY_corruption_level
				min = 0
				max = 100
			}
			add_stability = 0.04
			add_war_support = 0.02
			custom_effect_tooltip = CRY_corruption_decreased_tt
		}

		ai_will_do = {
			factor = 8
		}
	}

	# ── Purify the Crystal Mines ─────────────────────────────────────────────
	CRY_purify_mines = {
		icon = GFX_decision_generic_industry
		priority = 100

		allowed = { tag = CRY }

		available = {
			tag = CRY
			has_war = no
			NOT = { check_variable = { CRY_corruption_level < 1 } }
		}

		visible = {
			tag = CRY
			NOT = { has_country_flag = CRY_sombra_defeated }
		}

		cost = 100
		days_remove = 90

		complete_effect = {
			custom_effect_tooltip = CRY_purify_mines_tt
		}

		remove_effect = {
			add_to_variable = { CRY_corruption_level = -20 }
			clamp_variable = {
				var = CRY_corruption_level
				min = 0
				max = 100
			}
			add_resource = {
				type = crystals
				amount = 20
				state = 53
			}
			custom_effect_tooltip = CRY_corruption_decreased_tt
		}

		ai_will_do = {
			factor = 6
		}
	}

	# ── Root Out the Shadow's Agents ─────────────────────────────────────────
	CRY_root_out_shadow = {
		icon = GFX_decision_generic_spy_operation
		priority = 100

		allowed = { tag = CRY }

		available = {
			tag = CRY
			has_intelligence_agency = yes
			NOT = { check_variable = { CRY_corruption_level < 1 } }
		}

		visible = {
			tag = CRY
			NOT = { has_country_flag = CRY_sombra_defeated }
		}

		cost = 100
		days_remove = 45

		complete_effect = {
			custom_effect_tooltip = CRY_root_out_shadow_tt
		}

		remove_effect = {
			add_to_variable = { CRY_corruption_level = -12 }
			clamp_variable = {
				var = CRY_corruption_level
				min = 0
				max = 100
			}
			custom_effect_tooltip = CRY_corruption_decreased_tt
		}

		ai_will_do = {
			factor = 5
		}
	}
}
